version: '3'

includes:
  npm: 'tasks/npm.tasks.yml'
  prisma: 'tasks/prisma.tasks.yml'
  cruft: 'tasks/cruft.tasks.yml'

tasks:
  default:
    desc: show commands
    cmds:
      - task -l --sort none
